Chinese people use a system called pinyin to input Chinese characters on a computer keyboard. Pinyin is a way to spell Chinese words using the Roman alphabet. When you type the pinyin for a Chinese word into a computer, it will show a list of characters that match the pinyin spelling. You can then select the correct character from the list. For example, if you want to type the Chinese character for "dragon" on a computer, you would type "long" using the Roman alphabet. The computer would then show a list of characters that match the spelling "long", and you would select the character for "dragon" from the list. There are also other methods for inputting Chinese characters on a computer, such as handwriting recognition and using special input methods that are designed for typing Chinese characters more efficiently. Other languages also have ways to input characters on a computer. For example, Japanese has a system called kana input, which allows you to type Japanese characters using the Roman alphabet. Similarly, Korean has a system called hangul input, which allows you to type Korean characters using the Roman alphabet.