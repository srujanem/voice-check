Programs do no optimizations as they run besides programs that do JIT compiling , so they can only get slower . Usually this happens because the programmer did n't tell it to clean up parts of the program that should have been deleted . Modern OSes should n't get slower the longer you run them btw . That was mostly just a Windows thing , but even Windows should n't slow down after long periods of use now .